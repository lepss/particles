"use client";

import { OrbitControls, useFBO } from "@react-three/drei";
import {
  Canvas,
  createPortal,
  extend,
  useFrame,
  useLoader,
} from "@react-three/fiber";
import { useMemo, useRef } from "react";
import * as THREE from "three";

export const FBORender = (size) => {
  return <div>FBORender</div>;
};
